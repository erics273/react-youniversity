/* START SOLUTION */
export function hideFlash() {
    return (dispatch) => dispatch({type: 'HIDE_FLASH'});
}
/* ELSE
//This is a placeholder for any actions they may be relevent to UI changes
//such as hiding alerts or other visual changes to the UI.
END SOLUTION */